# نسخه 7.2.19

- رفع خطای زمان اجرا `wrapped C/C++ object ... has been deleted` در فایل `icon_manager.py`.
- جلوگیری از اجرای تأخیری پرداخت رابط روی `QLabel`، `QFrame` و سایر ویجت‌های موقت حذف‌شده.
- محدودکردن پرداخت کامل رابط به پنجره‌های سطح بالای زنده.
- افزودن کنترل زنده‌بودن QObject پیش از اجرای callbackهای `QTimer.singleShot`.
- ایمن‌سازی پردازش دکمه‌ها، تب‌ها، جدول‌ها، ورودی‌ها و فرم‌ها در برابر حذف هم‌زمان ویجت.
- حفظ کامل اصلاح فهرست بازشونده «نوع مکان» نسخه 7.2.18.
- بدون تغییر مخرب در دیتابیس یا حذف امکانات نسخه‌های قبلی.
